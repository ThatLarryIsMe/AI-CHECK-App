{
  "name": "proofmode-ai-monorepo",
  "private": true,
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  }
}
